server:
  port: ${PORT:8888}
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/faizalzafri/config-yml-repo
          username: faizalzafri
          password: github94f
          cloneOnStart: true